# SRL Dashboard (Streamlit)

## Quickstart
```bash
pip install -r requirements.txt
streamlit run app.py
```

- Radar uses Plotly polar (Expected vs Actual).
- Word cloud from strategy tags.
- Global chat limiter: 15 interactions/hour across all chat tabs.
